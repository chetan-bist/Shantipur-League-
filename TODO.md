- [x] Remove `History` link from navbar by updating `lib/constants.ts` (`NAV_LINKS`).
- [x] Verify standings table is already present (no change unless requested).

- [x] (Optional) Confirm `/history` route remains accessible after navbar removal.
